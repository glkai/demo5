<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myPlus.mapper.EmployeesMapper">
    <!---->
    <select id="selectMapById" resultType="map">
       select employee_id,last_name,job_id from t_employees where employee_id = #{id}
    </select>
</mapper>